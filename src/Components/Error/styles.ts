import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#C5D0D0',
        alignItems: 'center',
        paddingTop: 20,
    },
    errorText: {
        marginBottom: 10,
        color: 'red',
        textAlign: 'center',
        fontSize: 25,
    },
});
